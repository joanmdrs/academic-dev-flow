import React from "react";

const SectionContent = ({children}) => {

    return (
        <div style={{
            padding: '20px'
        }}> 
            {children}
        </div>
    )
}

export default SectionContent